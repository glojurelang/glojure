(let [handler (fn [w r] (. w (WriteHeader 200)))
      http-server (new go/net/http.Server
                       :Addr "localhost:8080"
                       :Handler (go/net/http.HandlerFunc handler))]
  (println (. http-server Handler)))
