;;;ERROR=invalid type for +: "hello"\nStack trace (most recent call first):\n* builtin + *\n	<unknown>:?:?\n

;;;TODO: better stack traces for tail calls
;;; something like:
;;;ERROR=invalid type for +: "hello"\nStack trace (most recent call first):\n* builtin + *\n	<unknown>:?:?\n<anonymous function>\n	stack00.in:3:13\nbad-use\n	stack00.in:5:3\n

(def add
  (fn [a b] (+ a b)))
(defn bad-use [x]
  (add x "hello"))
(bad-use 1)
